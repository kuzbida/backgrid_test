/*global define*/

define([
    'jquery',
    'backbone'
], function ($, Backbone) {
    'use strict';

    var GroupRouterRouter = Backbone.Router.extend({
        routes: {
        }

    });

    return GroupRouterRouter;
});
